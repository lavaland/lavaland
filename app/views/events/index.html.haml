%h1 Listing events

%table

  - @events.each do |event|
    %tr
      %td= event.title
      %td= event.location
      %td= event.date_and_time
      %td= event.description
      %td= link_to 'Read More', event
%br

